//
// This file is disabled in favor of ExamModeView.swift to avoid duplicate symbols.
//

#if false

import SwiftUI

struct ExamModeView: View {
    var body: some View {
        Text("Exam Mode Active")
            .font(.largeTitle)
            .padding()
    }
}

struct ExamModeView_Previews: PreviewProvider {
    static var previews: some View {
        ExamModeView()
    }
}

#endif
